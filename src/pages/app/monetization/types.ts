export type QueryParam = {
    app_id: string
}

export type MonetizationForm = {
    interstitial_interval_second: number
    enable_banner: boolean
    enable_interstitial: boolean
    enable_native: boolean
    enable_open: boolean
    enable_reward: boolean
}